- 配置依赖详见 `pom.xml`
- 编译（清除原编译文件）：`mvn clean compile`
- 运行：`mvn spring-boot:run`
- 连接本地 MySQL 数据库
- 根据 `sessionId` / `userId` 区分不同对话，不同对话间互相独立
- 能够初步理解对上文的指代从而实现较连贯的多轮对话（对话轮数有限，窗口大小是写死的需要手动修改）
- 展示答案内容及来源
- 有测试用接口
- 不支持多模态输入输出
- 未接入 Embedding 模型实现语义向量化